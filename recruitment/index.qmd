---
title: "Recruitment"
format: html
execute: 
  echo: false  
---

```{r}
#| label: load-packages
#| include: false

library(ggokabeito)

options(
  # set default colors in ggplot2 to colorblind-friendly
  # Okabe-Ito and Viridis palettes
  ggplot2.discrete.colour = ggokabeito::palette_okabe_ito(),
  ggplot2.discrete.fill = ggokabeito::palette_okabe_ito(),
  ggplot2.continuous.colour = "viridis",
  ggplot2.continuous.fill = "viridis",
  # set theme font and size
  book.base_family = "sans",
  book.base_size = 14
)

library(ggplot2)
library(dplyr)

# set default theme
theme_set(
  theme_minimal(
    base_size = getOption("book.base_size"),
    base_family = getOption("book.base_family")
  ) %+replace%
    theme(
      panel.grid.minor = element_blank(),
      legend.position = "bottom"
    )
)
```

Lorem ipsum.

```{r}
time <- seq(as.Date("2024-05-01"), as.Date("2024-12-31"), by = "months")
recruited <- seq(from = 10, to = 80, by = 10)

plot <- ggplot(tibble(time, recruited), aes(x = time, y = recruited)) +
  geom_bar(stat="identity") +
  scale_x_date(date_breaks = "2 month", date_labels =  "%b %Y") +
  ylab("Cumulative number of patients recruited")

plot
```

::: {layout-ncol="2"}
Lorem ipsum....
:::

